# ==============================================
# VPN ENTERPRISE - PRODUCTION ENV (EXAMPLE)
# Copy to .env.production and fill values.
# ==============================================

# Public domains (used by CORS + UI links)
# Replace example.com with your real domain
# API reads ALLOWED_ORIGINS (comma-separated). CORS_ORIGINS is kept as a legacy alias.
ALLOWED_ORIGINS=https://example.com,https://api.example.com,https://n8n.example.com
# Optional legacy alias (avoid ${VAR} interpolation; write the full value if you use this)
# CORS_ORIGINS=https://example.com,https://api.example.com,https://n8n.example.com

# n8n basic auth
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change_me_strong_password

# Optional: if your web app expects these at build/runtime
# For self-hosting, prefer same-origin so cookies/auth work without CORS pain.
# Nginx routes /api/* to the API container.
NEXT_PUBLIC_API_URL=https://example.com

# ==============================================
# SUPABASE (REQUIRED FOR CURRENT AUTH FLOW)
# ==============================================
# The API uses Supabase Auth (packages/auth) and Supabase DB access (packages/database).
# If these are missing, login/signup and many tenant/database routes will fail.
#
# Server-side (API / workers)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
# CRITICAL: keep this server-side only. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Web (public / exposed to the browser)
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Optional: Ollama CORS for browser clients (only matters if you call Ollama from the browser)
# OLLAMA_ORIGINS=https://example.com
